spring:
  datasource:
    url: jdbc:postgresql://localhost:1/emotion_test_metadata_only
    username: test
    password: test
    hikari:
      minimum-idle: 0
      initialization-fail-timeout: -1
  flyway:
    enabled: false
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        boot:
          allow_jdbc_metadata_access: false

eureka:
  client:
    enabled: false
