spring:
  flyway:
    enabled: true
    schemas: emotion_schema
    default-schema: emotion_schema
  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: validate

eureka:
  client:
    enabled: false
