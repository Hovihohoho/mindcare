import { useState } from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { DetailScreen, PageIntro, PrimaryAction, Row, Surface } from '../src/components/secondary-ui';
import { palette, space, type } from '../src/theme/tokens';
export default function DataRightsScreen() { const [exported, setExported] = useState(false); return <DetailScreen title="Quyền riêng tư"><PageIntro eyebrow="DỮ LIỆU CỦA BẠN" title="Quản lý dữ liệu cá nhân" text="Xem những gì MindCare có thể lưu trong trải nghiệm này và lựa chọn cách quản lý." /><Surface><Row icon="document-text-outline" title="Tải bản sao dữ liệu" detail="Check-in và tùy chọn của bạn" onPress={() => setExported(true)} /><Row icon="eye-off-outline" title="Quyền truy cập sức khỏe" detail="Chỉ đọc khi bạn cho phép" /><Row icon="trash-outline" title="Xóa dữ liệu minh họa" detail="Không thể hoàn tác trong ứng dụng thật" last /></Surface>{exported ? <View style={styles.confirm}><Text style={styles.confirmTitle}>Đã chuẩn bị bản sao mẫu</Text><Text style={styles.confirmText}>Prototype không tạo hoặc tải tệp thật.</Text></View> : null}<PrimaryAction label="Xem chính sách quyền riêng tư" icon="shield-checkmark-outline" /></DetailScreen>; }
const styles = StyleSheet.create({ confirm: { backgroundColor: palette.mint, borderColor: palette.line, borderWidth: 1, marginBottom: space.md, padding: space.md }, confirmTitle: { color: palette.ink, fontSize: type.label, fontWeight: '700' }, confirmText: { color: palette.muted, fontSize: 12, marginTop: 3 } });
