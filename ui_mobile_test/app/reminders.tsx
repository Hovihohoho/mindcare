import Ionicons from '@expo/vector-icons/Ionicons';
import { useState } from 'react';
import { Pressable, StyleSheet, Switch, Text, View } from 'react-native';
import { DetailScreen, PageIntro, PrimaryAction, Surface } from '../src/components/secondary-ui';
import { palette, radius, space, type } from '../src/theme/tokens';
export default function RemindersScreen() { const [checkin, setCheckin] = useState(true); const [sleep, setSleep] = useState(false); const [saved, setSaved] = useState(false); return <DetailScreen title="Nhắc nhở"><PageIntro eyebrow="THÓI QUEN" title="Chọn lúc cần được nhắc" text="Các nhắc nhở nên ít nhưng đúng lúc. Bạn có thể thay đổi bất cứ khi nào." /><Surface><Reminder icon="happy-outline" title="Check-in cảm xúc" detail="Mỗi ngày lúc 20:30" value={checkin} onChange={setCheckin} /><Reminder icon="bed-outline" title="Giờ đi ngủ" detail="Nhắc chuẩn bị ngủ lúc 22:30" value={sleep} onChange={setSleep} last /></Surface><View style={styles.time}><Ionicons color={palette.primary} name="time-outline" size={20} /><Text style={styles.timeText}>Khung giờ ưu tiên: 20:30</Text><Pressable accessibilityRole="button" style={styles.edit}><Text style={styles.editText}>Chỉnh sửa</Text></Pressable></View><PrimaryAction label={saved ? 'Đã lưu thay đổi' : 'Lưu nhắc nhở'} icon={saved ? 'checkmark' : 'save-outline'} onPress={() => setSaved(true)} /></DetailScreen>; }
function Reminder({ icon, title, detail, value, onChange, last = false }: { icon: keyof typeof Ionicons.glyphMap; title: string; detail: string; value: boolean; onChange(value: boolean): void; last?: boolean }) { return <View style={[styles.reminder, !last && styles.reminderRule]}><Ionicons color={palette.muted} name={icon} size={20} /><View style={{ flex: 1 }}><Text style={styles.reminderTitle}>{title}</Text><Text style={styles.reminderDetail}>{detail}</Text></View><Switch trackColor={{ false: palette.chartMuted, true: palette.primary }} value={value} onValueChange={onChange} /></View>; }
const styles = StyleSheet.create({ reminder: { alignItems: 'center', flexDirection: 'row', gap: space.sm, minHeight: 76, paddingHorizontal: space.md }, reminderRule: { borderBottomColor: palette.line, borderBottomWidth: StyleSheet.hairlineWidth }, reminderTitle: { color: palette.ink, fontSize: type.label, fontWeight: '700' }, reminderDetail: { color: palette.muted, fontSize: 12, marginTop: 3 }, time: { alignItems: 'center', backgroundColor: palette.sky, borderColor: palette.line, borderRadius: radius.md, borderWidth: 1, flexDirection: 'row', gap: space.sm, marginBottom: space.md, padding: space.md }, timeText: { color: palette.ink, flex: 1, fontSize: 13 }, edit: { padding: 4 }, editText: { color: palette.primary, fontSize: 12, fontWeight: '800' } });
