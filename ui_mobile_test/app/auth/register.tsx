import { useRouter } from 'expo-router';
import { useState } from 'react';
import { Pressable, StyleSheet, Text, TextInput, View } from 'react-native';
import { DetailScreen, PrimaryAction } from '../../src/components/secondary-ui';
import { palette, radius, space, type } from '../../src/theme/tokens';
export default function RegisterScreen() { const router = useRouter(); const [name, setName] = useState(''); return <DetailScreen title="Đăng ký"><View style={styles.body}><Text style={styles.title}>Bắt đầu theo nhịp của bạn</Text><Text style={styles.text}>Tạo tài khoản để lưu các tùy chọn trên thiết bị của bạn trong phiên bản ứng dụng thật.</Text><Text style={styles.label}>TÊN HIỂN THỊ</Text><TextInput onChangeText={setName} placeholder="Bạn muốn được gọi là gì?" placeholderTextColor={palette.muted} style={styles.input} value={name} /><PrimaryAction label="Tiếp tục" onPress={() => router.push('/auth/verify')} /><Pressable onPress={() => router.back()} style={styles.link}><Text style={styles.linkText}>Đã có tài khoản? Đăng nhập</Text></Pressable></View></DetailScreen>; }
const styles = StyleSheet.create({ body: { paddingTop: space.xl }, title: { color: palette.ink, fontSize: 28, fontWeight: '700', letterSpacing: -.7 }, text: { color: palette.muted, fontSize: type.body, lineHeight: 23, marginBottom: space.xl, marginTop: space.sm }, label: { color: palette.muted, fontSize: type.overline, fontWeight: '800', letterSpacing: .7, marginBottom: 7 }, input: { backgroundColor: palette.surface, borderColor: palette.line, borderRadius: radius.sm, borderWidth: 1, color: palette.ink, fontSize: type.body, height: 50, marginBottom: space.md, paddingHorizontal: space.sm }, link: { alignSelf: 'center', padding: space.md }, linkText: { color: palette.primary, fontSize: type.label, fontWeight: '700' } });
