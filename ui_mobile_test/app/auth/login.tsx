import { useRouter } from 'expo-router';
import { useState } from 'react';
import { Pressable, StyleSheet, Text, TextInput, View } from 'react-native';
import { Screen } from '../../src/components/ui';
import { PrimaryAction } from '../../src/components/secondary-ui';
import { palette, radius, space, type } from '../../src/theme/tokens';
export default function LoginScreen() { const router = useRouter(); const [email, setEmail] = useState(''); return <Screen><View style={styles.wrap}><Text style={styles.brand}>MindCare</Text><Text style={styles.title}>Chào mừng trở lại</Text><Text style={styles.text}>Đăng nhập để tiếp tục theo dõi sức khỏe và cảm xúc của bạn.</Text><Text style={styles.label}>EMAIL</Text><TextInput autoCapitalize="none" keyboardType="email-address" onChangeText={setEmail} placeholder="you@example.com" placeholderTextColor={palette.muted} style={styles.input} value={email} /><PrimaryAction label="Đăng nhập" onPress={() => router.replace('/(tabs)/home')} /><Pressable onPress={() => router.push('/auth/register')} style={styles.link}><Text style={styles.linkText}>Chưa có tài khoản? Đăng ký</Text></Pressable></View></Screen>; }
const styles = StyleSheet.create({ wrap: { flex: 1, justifyContent: 'center', padding: space.page }, brand: { color: palette.primary, fontSize: type.title, fontWeight: '800' }, title: { color: palette.ink, fontSize: 30, fontWeight: '700', letterSpacing: -.8, marginTop: space.xl }, text: { color: palette.muted, fontSize: type.body, lineHeight: 23, marginBottom: space.xl, marginTop: space.sm }, label: { color: palette.muted, fontSize: type.overline, fontWeight: '800', letterSpacing: .7, marginBottom: 7 }, input: { backgroundColor: palette.surface, borderColor: palette.line, borderRadius: radius.sm, borderWidth: 1, color: palette.ink, fontSize: type.body, height: 50, marginBottom: space.md, paddingHorizontal: space.sm }, link: { alignSelf: 'center', padding: space.md }, linkText: { color: palette.primary, fontSize: type.label, fontWeight: '700' } });
