# MindCare User Frontend

```bash
npm install
npm run dev
```

The app uses `VITE_API_URL` (defaults to `http://localhost:8080`) for the API gateway. Authentication calls are live; feature data remains local until the corresponding backend services are implemented.
